# 移动端下载 App 设计稿（uniapp + 后端视频处理服务）

## 一、总体架构

**核心前提**：手机端跑不了 ffmpeg/yt-dlp/Whisper，所有视频处理都在服务器完成。uniapp 只是「壳」——负责 UI、文件上传下载、调后端接口、展示结果。

```
用户(手机) ──上传视频/粘贴链接──▶ uniapp 前端
                                     │ 调 API
                                     ▼
                            后端服务(Python + ffmpeg + yt-dlp)
                                     │
                    ┌────────────────┼────────────────┐
                    ▼                ▼                ▼
                阿里云 OSS       ffmpeg 处理      云 ASR 接口
              (文件存储/CDN)   (去水印/转码等)   (语音转文字)
```

**技术栈选型**：

| 层 | 选型 | 理由 |
|----|------|------|
| 前端 | uniapp（Vue3） | 一套代码编译 App/小程序/H5 |
| 后端 | **Python FastAPI**（推荐）或 Node.js | Python 视频处理生态好（moviepy/ffmpeg-python），未来本地 Whisper 也是 Python |
| 视频处理 | ffmpeg（子进程调用） | 业界标准，覆盖所有功能 |
| 视频下载 | yt-dlp | 复用桌面版现成方案 |
| 存储 | 阿里云 OSS | 大文件上传下载 + CDN |
| 任务队列 | 异步任务 + 轮询/WebSocket | 视频处理耗时，不能同步等 |
| ASR | 阿里云/讯飞语音识别（先云 API） | 按量付费、接入快，预留本地 Whisper 扩展位 |

---

## 二、各功能详细设计

### 1. 视频下载

**实现**：用户粘贴链接 → 后端 yt-dlp `--dump-json` 解析 → 选清晰度 → yt-dlp 下载 → 转存 OSS → 返回下载链接。

**难点**：
- 反爬 cookie：服务器统一维护登录 cookie，或提供「上传 cookie」入口。
- 大文件带宽，下载走 OSS/CDN。
- 版权合规风险（App Store 上架尤其严）。

**难度**：★★★☆☆

---

### 2. 视频去水印（重点）

定位方式：**手动框选为主，AI 检测为高级功能**。

#### 方案对比

| 方案 | 原理 | 优点 | 缺点 | 难度 | 效果 |
|------|------|------|------|------|------|
| **A. 裁剪 crop** | 裁掉水印所在边 | 零痕迹、快 | 损失画面；水印须在边缘 | ★ | 干净但少边角 |
| **B. 模糊 boxblur** | 水印位置打模糊块 | 通用、快 | 留模糊块，不美观 | ★★ | 糊住但看得出 |
| **C. delogo 插值** | 周围像素插值填充 | 比模糊自然 | 水印大时糊 | ★★ | 较自然有痕迹 |
| **D. AI 修复 Inpainting** | 模型逐帧检测+修复 | 接近无痕 | 需 GPU、贵、慢、复杂 | ★★★★★ | 最好 |

#### 推荐实现路径

- **第一期（手动框选）**：上传视频 → 抽帧预览 → 手动框选水印位置 → 后端 ffmpeg 用 A/B/C 之一处理。
- **第二期（AI 高级功能）**：AI 修复模型（ProPainter），付费高级功能，需 GPU 服务器。

**核心难点**：AI 算力成本（最大变量）；手动定位要准；多水印/移动水印场景。

---

### 3. 视频修改 MD5

**实现**：MD5 是文件内容哈希，改任意字节就变。推荐**文件末尾追加几个字节**（毫秒级、无损、不影响播放）。

**难点**：几乎没有。注意灰产/合规风险。

**难度**：★☆☆☆☆

---

### 4. 导出字幕（上传视频/音频）

**实现**：
1. 有内嵌字幕的视频：ffmpeg `-map 0:s` 直接提取。
2. 无字幕视频/音频：ASR 语音转文字 → 生成 srt/vtt（先云 API，后本地 Whisper）。

**难点**：ASR 准确率、长音频切分、成本、时间戳对齐。

**难度**：★★★☆☆

---

### 5. 视频加水印

**实现**：ffmpeg `drawtext`（文字）/ `overlay`（图片），可设位置/透明度/大小，可选动态水印。

**难点**：几乎没有（中文水印需装中文字体）。

**难度**：★☆☆☆☆

---

### 6. 导出语音（提取音频）

**实现**：ffmpeg `-vn` 提取音轨，转 mp3/m4a。

**难点**：几乎没有。

**难度**：★☆☆☆☆

---

## 三、关键难点汇总（跨功能）

1. 移动端无法本地处理视频 → 全走后端，上传下载大文件是瓶颈。
2. 大文件传输：分片上传、断点续传、OSS 直传。
3. 任务异步化：提交任务 → 轮询/WebSocket 拿进度 → 完成后下载。
4. 小程序限制：微信小程序对下载/保存相册/上传大文件限制多，部分功能需降级。
5. 合规：视频下载、改 MD5 有灰产/版权风险，App Store 上架难。
6. 去水印 AI 算力：GPU 成本是最大变量，按量计费或付费高级功能。

---

## 四、分阶段开发计划

| 阶段 | 内容 |
|------|------|
| 阶段 1 | 后端骨架 + 视频下载（FastAPI + ffmpeg + yt-dlp + OSS） |
| 阶段 2 | 改 MD5 + 加水印 + 导出语音（三个最简单功能） |
| 阶段 3 | 去水印（手动框选 + delogo/裁剪/模糊） |
| 阶段 4 | 导出字幕（内嵌字幕提取 + 云 ASR） |
| 阶段 5 | uniapp 前端（首页/工具箱/我的） |
| 阶段 6 | AI 去水印（可选，GPU + ProPainter） |

---

## 五、需要后续确认的点

1. 后端语言定 Python 还是 Node（推荐 Python）。
2. ASR 用哪家云 API（阿里云/讯飞/百度）。
3. 上架渠道（App Store 大概率过不了审，是否只做安卓 APK + 小程序 + H5）。
4. 是否现在就上 AI 去水印，还是先手动版跑起来。
